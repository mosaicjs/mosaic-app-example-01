import React from 'react';
import { ListItemView } from 'mosaic-ui-list';

export default class ResourceListItemView extends ListItemView {
    _renderContent(){
        return super._renderContent();
    }
}